{
  "name": "@gitnexus/prep-action",
  "version": "2.0.0",
  "private": true,
  "description": "GitNexus prep step — bundles + Context Pack + system prompt for Claude PR review.",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --license licenses.txt && node -e \"require('fs').cpSync('src/templates','dist/templates',{recursive:true})\"",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.7.0",
    "form-data": "^4.0.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.5.0",
    "vitest": "^1.0.0"
  }
}
