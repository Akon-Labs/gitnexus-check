{
  "name": "@gitnexus/check-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitNexus PR review — calls the GitNexus Hub blast-radius endpoints and posts a markdown comment on the PR.",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --license licenses.txt",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "check-dist": "node scripts/check-dist.mjs"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/github": "^6.0.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "openai": "^6.42.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
